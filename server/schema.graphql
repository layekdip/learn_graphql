type Query {
#  greeting: String
  job(id: ID!): Job
  jobs: [Job]
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  id: ID!
  date: String!
  title: String
  company: Company!
  description: String
}